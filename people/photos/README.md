Headshots for the people directory.

- `placeholder.png` — default silhouette when a person has no photo.
- Add `{id}.jpg` (or `.png`) and set `"photo": "{id}.jpg"` on that person in `catalog/desx-people.json`.

Prefer a square crop, face near center, under about 1 MB.
